import * as React from 'react';
import { PageSection, Title } from '@patternfly/react-core';

const MainPage: React.FunctionComponent = () => (
  <PageSection>
    <Title headingLevel="h1" size="lg">This is where SSO would go.</Title>
  </PageSection>
)

export { MainPage };
