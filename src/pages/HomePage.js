import React from "react";
import { PageSection } from '@patternfly/react-core';

const HomePage = () => (
  <PageSection>
    HomePage in single-sign-on
  </PageSection>
);

export default HomePage;
